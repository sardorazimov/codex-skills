# Minimal Example

This directory contains the smallest working codex-sk examples.

## Python

```bash
PYTHONPATH=bindings/python/src python3 examples/minimal/python_example.py
```

The example uses the local Python SDK only. It does not require Rust FFI.
