# Integration Tests

Cross-crate and end-to-end tests belong here when they are too broad for a
single Rust crate or Python package test.

Prefer focused integration tests that exercise public behavior rather than
private implementation details.
